# Pinned to the versions this repo was tested with (Python 3.12).
# GPU training: install the CUDA build of torch/torchvision matching your driver first
#   (pytorch.org/get-started), then `pip install -r requirements.txt`.
torch==2.14.0
torchvision==0.29.0
ultralytics==8.4.146
opencv-python-headless==4.13.0.92
numpy==2.4.4
pillow==12.1.1
pyyaml==6.0.3
imagehash==4.3.2
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
anthropic==1.4.0
